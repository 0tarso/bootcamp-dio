**1 - Instalação**
  - npm install -D typescript
  - npm install -D tsx
  - npm install -D tsup

**2 - Config**
  - Gerar o tsconfig
    ->> npx tsc --init

**3 - Scripts**
  - Gerar build final
    ->> npx tsc 
    <br> 
  - Modo watch com ts
    ->> tsx --watch src/index.ts